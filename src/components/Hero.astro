<section
  class="bg-(--primary-color) text-(--secondary-color) px-4 md:px-8 flex h-screen flex-col justify-between"
  id="inicio"
>
  <div
    class="max-w-6xl mx-auto flex flex-col text-center flex-1 justify-center"
  >
    <h1 class="text-black text-2xl md:text-5xl mb-4 animate-fade-in-up">Yésica Fontealba</h1>
    <p class="mb-28 text-base animate-fade-in-up animation-delay-200">
      Capturando momentos únicos con pasión y profesionalismo.
    </p>
    <div class="flex justify-center items-center flex-col animate-fade-in-up animation-delay-400">
      <p class="text-xs tracking-widest mb-4">SCROLL</p>
      <div class="w-0.5 h-10 bg-(--secondary-color) mb-8"></div>
      <a
        href="#portfolio"
        class="text-(--secondary-color) border-2 border-dashed border-(--secondary-color) w-3xs px-6 py-3 rounded-3xl font-normal hover:bg-opacity-80 transition inline-block"
        >Ver Portafolio</a
      >
    </div>
  </div>
</section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
  }
</style>
